extends ../../layout

block header
    header.bar.bar-nav
        a.button.button-link.button-nav.pull-left.open-panel
        h1.title 注册

block content
    .content.native-scroll
        .content-padded.text-center
            p #{message}
        .content-block
            .row
                .col-100
                    a.button.button-big.button-fill(href='#{href}') #{buttonName}